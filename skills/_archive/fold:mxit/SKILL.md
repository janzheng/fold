---
name: fold:mxit
description: "Renamed to fold:tasks. Use /fold:tasks — same skill, same content, shorter address. fold:mxit is kept as a redirect so the old muscle-memory name still works."
---

# fold:mxit (renamed)

This skill is now **`fold:tasks`**. Same content, same behavior — just dropped the `:mxit` segment as part of flattening the fold namespace.

Use `/fold:tasks` instead. If a doc, conversation, or muscle-memory typo lands here, route to `/fold:tasks` and continue from there.

## Sibling renames

- `fold:mxit` → `fold:tasks`
- `fold:mxit:brief` → `fold:brief`
- `fold:mxit:explore` → `fold:explore`
- `fold:mxit:run` → `fold:run`

## See also

- `/fold:tasks` — the actual skill (mxit task tracking, TASKS family, briefs/tickets/SOPs, runner)
