# Archived Project Workflows

On 2026-09-08 Jan archived `proj:autorefine` because it was rarely used.
Its complete source is preserved unchanged in `skills/proj:autorefine/` here,
outside the active repository `skills/` directory. Do not copy this archive into
an agent's skill loader or restore it during ordinary sync.

To restore on explicit request: move the skill back to the repository's active
`skills/proj:autorefine/`, restore its owner in MCP Hub's
`skills-workshop/proj-sources.json`, refresh the registry and recovery mirror,
then sync the chosen installs. Check compatibility before resuming old workflows.
Archived copies and historical REFINE.md records are not active instructions.
